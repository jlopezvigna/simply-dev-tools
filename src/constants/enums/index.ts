export enum SortValue {
  NoSort = "no-sort",
  Az = "asc",
  Za = "desc",
}

export enum SortLabel {
  NoSort = "No Sort",
  Az = "Sort A->z",
  Za = "Sort z->A",
}

export enum LoremIpsumValues {
  PARAGRAPHS = "paragraphs",
  SETENCES = "sentences",
  WORDS = "words",
}
